/*global angular */

// Declare app level module which depends on ngRoute and ngResoruce (which will be used in Level 3 for the first time)
angular.module('NoteWrangler', ['ngRoute', 'ngResource']).config(function () {
    'use strict';
});